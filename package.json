{
  "name": "interface-finetuning-extension",
  "version": "0.1.1",
  "description": "Chrome extension MVP that lets users refine AI-generated UIs region-by-region and export copy-ready prompts for Claude Code and Codex.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "build:companion": "vite build --config vite.companion.config.ts",
    "dev:iframe": "npm run build:companion && vite --config vite.iframe.config.ts",
    "build:iframe": "npm run build:companion && vite build --config vite.iframe.config.ts",
    "preview:iframe": "vite preview --config vite.iframe.config.ts"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.28",
    "@types/chrome": "^0.0.270",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.5.4",
    "vite": "^5.4.0"
  }
}
